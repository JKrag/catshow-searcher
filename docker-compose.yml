services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: catz
      POSTGRES_PASSWORD: catz
      POSTGRES_DB: catz
    ports:
      - "5432:5432"
    volumes:
      - catz_pgdata:/var/lib/postgresql/data

volumes:
  catz_pgdata:
